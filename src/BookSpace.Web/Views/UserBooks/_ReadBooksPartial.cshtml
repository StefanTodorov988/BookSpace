@model IEnumerable<UserBookViewModel>

@if (Model == null)
{
    <h3>Sorry pal! You haven't read any book yet!</h3>
}
else
{
    @foreach (var b in Model)
    {
         @await Html.PartialAsync("_BookPartial", b);
    }
}
